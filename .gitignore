# Ignoruj celou složku service/data/ (produkty, směny, účty, ID)
service/data/

# Zachovat pouze categories.json pokud existuje
!service/data/categories.json

# Ignoruj node_modules v service/
service/node_modules/
service/package-lock.json
node_modules/
package-lock.json

# Logy a dočasné soubory
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Systémové soubory
.DS_Store
Thumbs.db

# VSCode workspace nastavení
.vscode/

# Dočasné soubory a zálohy
*.tmp
*.swp
*.bak

# Ignoruj service_account.json (pokud obsahuje citlivé údaje)
service/service_account.json

# Lokální konfigurace a OAuth tokeny (povolíme pouze *.example.json)
service/local-config.json
service/local-configs/
!service/local-configs/*.example.json
!service/local-configs/README.md
!service/local-configs/.gitkeep

# Lokalni zalohy a docasna data obnovy
service/backups/
service/data__before_restore_*/

# Ignoruj vše v client/scripts/ co není JS
client/scripts/*.map
client/scripts/*.tmp
client/scripts/*.bak

# Ignoruj vše v client/styles/ co není CSS
client/styles/*.map
client/styles/*.tmp
client/styles/*.bak

service/data.zip
